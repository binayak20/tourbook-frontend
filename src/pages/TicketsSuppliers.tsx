import { Containers } from '@/containers';

const TicketsSuppliers = () => (
	// <AccessBoundary to='TICKETS' isDefaultFallback>
	<Containers.TicketsSuppliers />
	// </AccessBoundary>
);

export default TicketsSuppliers;
